# Model Card

SeamScope does not use a trained statistical or machine-learning model.

Its behavior comes from deterministic luminance conversion, Sobel filtering, additive user bias, and dynamic programming. It should not be described as recognizing subjects, importance, saliency, or semantic meaning. A high-contrast detail can receive high energy even when unimportant, while a low-contrast subject can be removed unless the user protects it.
