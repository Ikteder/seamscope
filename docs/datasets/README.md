# Dataset Card

SeamScope uses no collected, downloaded, labeled, training, validation, or test dataset.

The default scene is drawn procedurally at runtime with Canvas 2D primitives. User-selected images remain local to the browser and are not retained by the repository or treated as evaluation data. Consequently there is no dataset license, split, leakage risk, or preprocessing corpus to report.
