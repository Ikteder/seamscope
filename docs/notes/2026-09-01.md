# Working Notes: 2026-09-01

## Selection

Seed `20260901` produced mode roll `0.8010605011932569`, selecting the existing-project improvement branch. SeamScope was clean, public, within the software development lane, and had a specific unimplemented next step backed by a testable algorithmic need.

## Implementation observations

- Forward energy needed directional transition costs, so it could not be represented by the existing scalar Sobel field alone. The interface continues to display Sobel contrast and explains that the selected forward seam also prices new adjacency.
- Preview state now records both axis and method. Changing the method invalidates the path before removal.
- The target run snapshots one method for the whole operation and disables method and axis controls until completion.
- The same transpose route supports horizontal forward seams, avoiding a second algorithm implementation.

## Evidence boundary

The generated benchmark favors reproducibility and license safety. Its adjacency metric is aligned with the forward-energy objective and therefore checks implementation behavior, but it is not independent proof of perceptual superiority. Exact observed results are recorded in the experiment note.
