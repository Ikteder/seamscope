# Working Notes — 2026-08-26

## Selection

- Local seed: `20260826`.
- Generator: Python 3.14.7 `random.Random(seed)`.
- Mode roll: `0.47447814443256064`; the documented threshold `< 0.70` selected a new project.
- Category 3 (LLM/SLM/agents/evaluation) was excluded because the previous scheduled run used it.
- Uniform draw from the remaining 16 categories returned zero-based index `1`, category 2: computer vision and image/video processing.
- No redraw was used.

## Inventory observations

- The workspace included 22 Git repositories and 29 public repositories visible through the GitHub public API.
- Existing computer-vision work focuses on classification, robustness, edge-AI benchmarking, and model export.
- SeamScope differs in problem (content-aware retargeting), intended user (designers and learners), algorithm (Sobel energy plus dynamic programming), interface (interactive browser canvas), and stack (dependency-free JavaScript).
- Dirty working trees in `fit-tiles` and `po2-qat-deployment-studio` were left untouched. Hardware/ECE-oriented SPARQ material was excluded.

## Implementation notes

- Rust was considered to vary the portfolio stack but `rustc` and `cargo` were not installed. The category and product were retained using browser-native JavaScript so the project remained finishable and fully verifiable.
- Core image operations were kept free of DOM dependencies to support deterministic Node tests.
- Horizontal seams reuse the vertical primitive through transposition, reducing duplicate algorithm surface area.
- The demo is generated in Canvas 2D to avoid asset provenance and license ambiguity.

## Browser defect found and fixed

The first browser smoke test failed during energy rendering because `Math.max(...largeArray)` exceeded the browser call stack. The reduction now uses an explicit loop. The same review found that a transparent mask was copied with `putImageData` rather than composited; it now renders through an overlay canvas.

## Open questions

- Whether forward energy provides a visibly better default for architecture, typography, and other strongly structured images.
- Whether a Web Worker is worthwhile above the current 900-pixel input cap.
- Whether mask persistence across reset/export would improve real workflows or add confusing state.
